{
    "name": "@fixentropy-io/three-tier-asserter",
    "module": "index.ts",
    "type": "module",
    "version": "0.0.1",
    "scripts": {
        "format": "biome format --write",
        "lint": "biome lint",
        "lint:fix": "biome lint --write",
        "check": "biome check",
        "check:fix": "biome check --write",
        "test": "bun test --watch"
    },
    "devDependencies": {
        "@biomejs/biome": "^1.9.0",
        "@types/bun": "latest",
        "typedoc": "^0.26.7"
    },
    "peerDependencies": {
        "typescript": "^5.5.0"
    },
    "trustedDependencies": ["@biomejs/biome"],
    "dependencies": {
        "@fixentropy-io/type": "^0.0.6"
    }
}
